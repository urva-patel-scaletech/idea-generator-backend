export enum AssistantCategory {
  IDEA = 'idea',
  STRATEGY = 'strategy',
  EXECUTION = 'execution',
  GROWTH = 'growth',
}

export enum ThreadStage {
  IDEA = 'idea',
  STRATEGY = 'strategy',
  EXECUTION = 'execution',
  GROWTH = 'growth',
}

export enum MessageSender {
  USER = 'user',
  ASSISTANT = 'assistant',
}
